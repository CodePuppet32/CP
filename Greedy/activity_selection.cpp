#include <bits/stdc++.h>
using namespace std;

void algo(int arr[], int size) {
    
}

int main() {
    int arr[] = {};
    int size = sizeof(arr)/sizeof(arr[0]);
    algo(arr, size);
    return 0;
}